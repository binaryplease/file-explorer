# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
data

# Nix build artifacts
result
.direnv
.playwright-cli
# Brand/logo scratch (creative explorations)
brand

# Local dev notebook: a chronological working log, run logs, and ideas not yet
# promoted. Local-only scratch; nothing committed may depend on it. Requirements,
# decisions and research are NOT here — they live in the committed docs/.
.nightshift
